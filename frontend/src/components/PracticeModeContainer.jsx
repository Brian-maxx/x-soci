export default function PracticeModeContainer({ title, description }) {
    return (
        <div className="content">
            <p className="content-title">{title}</p>
            <p className='content-description'>{description}</p>
        </div>
    )
}